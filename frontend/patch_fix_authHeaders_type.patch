From abcd1234abcd1234abcd1234abcd1234abcd1234 Mon Sep 17 00:00:00 2001
From: AlgoDatta Bot <bot@algodatta.com>
Date: Fri, 15 Aug 2025 17:00:16 +0000
Subject: [PATCH] fix(frontend): type authHeaders() to satisfy HeadersInit

---
 frontend/lib/api.ts | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/frontend/lib/api.ts b/frontend/lib/api.ts
index 1111111..2222222 100644
--- a/frontend/lib/api.ts
+++ b/frontend/lib/api.ts
@@ -1,6 +1,6 @@
 export const apiBase = () => process.env.NEXT_PUBLIC_API_BASE || "http://localhost:8000";
 
 export const getToken = () => (typeof window !== 'undefined') ? localStorage.getItem("token") || "" : "";
 
 export const setToken = (t:string) => { if(typeof window!=='undefined') localStorage.setItem("token", t); };
 
-export const authHeaders = () => {
+export const authHeaders = (): Record<string, string> => {
   const t = getToken();
   return t ? { Authorization: `Bearer ${t}` } : {};
 };
-- 
2.46.0
