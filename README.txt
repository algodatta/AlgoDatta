📘 How to Run the Pre-Built Algo Trading Platform

1. Edit backend/.env
   - Replace FERNET_SECRET_KEY with your generated key

2. In terminal:
   docker-compose pull
   docker-compose up

3. Open in browser:
   - Frontend: http://localhost:3000
   - Backend:  http://localhost:8000
